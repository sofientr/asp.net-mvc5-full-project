@model IEnumerable<Inspinia_MVC5.Models.Facturation>

@{
    ViewBag.Title = "Index";
}

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-sm-4">
        <h2>Index</h2>
        <ol class="breadcrumb">
            <li class="active">
                <strong>Facturation</strong>
            </li>
        </ol>
    </div>
</div>


<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-lg-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h5>Liste Facturation</h5>
					<div class="ibox-tools">
						@Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary btn-xs"})
					</div>
                </div>
                <div class="ibox-content">

                    <table class="table table-striped table-bordered table-hover dataTables-example">
                        <thead>
                            <tr>
                                <th>
                                    @Html.DisplayNameFor(model => model.DateFact)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.NomFact)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Commande.CmdID)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Encaissement.EncaissID)
                                </th>
                                <th>
                                    @Html.DisplayNameFor(model => model.Projets.CODE)
                                </th>
                                <th></th>
                            </tr>
                            </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.DateFact)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.NomFact)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Commande.CmdID)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Encaissement.EncaissID)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.Projets.CODE)
                                    </td>
                                    <td>
                                        @Html.ActionLink("Details", "Details", new { id = item.FactID }, new { @class = "btn btn-primary btn-sm" })
                                        @Html.ActionLink("Edit", "Edit", new { id = item.FactID }, new { @class = "btn btn-white btn-sm" })
                                        @Html.ActionLink("Delete", "Delete", new { id = item.FactID }, new { @class = "btn btn-white btn-sm" })
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>

                </div>
            </div>
        </div>
    </div>
 </div>

@section Styles {
    @Styles.Render("~/Content/plugins/dataTables/dataTablesStyles")
}
@section Scripts {
    @Scripts.Render("~/plugins/dataTables")
    <script type="text/javascript">
        $(document).ready(function () {

            $('.dataTables-example').DataTable({
                pageLength: 25,
                dom: '<"html5buttons"B>lTfgitp',
                buttons: [
                    { extend: 'copy' },
                    { extend: 'csv', title: 'liste facturations' },
                    { extend: 'excel', title: 'liste facturations' },
                    { extend: 'pdf', title: 'liste facturations' },

                    {
                        extend: 'print',
                        customize: function (win) {
                            $(win.document.body).addClass('white-bg');
                            $(win.document.body).css('font-size', '10px');

                            $(win.document.body).find('table')
                                    .addClass('compact')
                                    .css('font-size', 'inherit');
                        }
                    }
                ]

            });



        });

    </script>
}








